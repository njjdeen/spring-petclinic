trigger:
- '*'

pool:
  name: 'windows-agent'

steps:

- task: PowerShell@2
  displayName: print message
  inputs:
    targetType: inline
    script: echo "Pipeline triggered succesfully"
    workingDirectory: 